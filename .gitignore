# ─────────────────────────────────────────────────────────────────────────────
# ПУБЛИЧНЫЙ репозиторий (GitHub Pages). Разрешено только то, что перечислено ниже.
#
# Правило: игнорируем ВСЁ, разрешаем поимённо — чтобы файл с персональными
# данными не попал в публикацию по невнимательности.
#
# index.html собирается скриптом knowledge-base/_scripts/svod-anon-build.py
# из приватных исходников и содержит только обезличенные агрегаты.
# ─────────────────────────────────────────────────────────────────────────────
*

!.gitignore
!.nojekyll
!README.md
!index.html
